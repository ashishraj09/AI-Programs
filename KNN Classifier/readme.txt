Instruction to run KKN Classifier application:

1) Run the below command in the command line.
	java - jar KNNClassifier.jar
2) Please make sure the command is executed in the same directory as the file. 

The program will request the file names of the training and test file.
Enter the k-value, and on successful execution, the output will be displayed.
